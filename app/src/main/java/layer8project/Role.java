package layer8project;

// This enum defines the different roles a user can have in the system: USER, ADMIN, and BANNED. Each role determines the level of access and permissions a user has within the application.

public enum Role {
    USER, ADMIN, BANNED
}